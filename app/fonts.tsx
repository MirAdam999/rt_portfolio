import { Inter } from 'next/font/google'

export const fine_font = Inter({
    subsets: ['latin'],
    weight: ['400']
})

export const mid_font = Inter({
    subsets: ['latin'],
    weight: ['600']
})

export const heavy_font = Inter({
    subsets: ['latin'],
    weight: ['800']
})
